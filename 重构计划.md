toolName: no_need_execute

status: no_need_execute





toolName: view_folder

status: success

d:\_yvmou\GitHub\minecraft-plugin\_2025Code\PlayerTaskX




toolName: view_files

status: success


filePath: d:\_yvmou\GitHub\minecraft-plugin\_2025Code\PlayerTaskX\core\src\main\java\com\playerPlugin\playerTaskX\commands\CommandRegister.java




toolName: view_files

status: success


filePath: d:\_yvmou\GitHub\minecraft-plugin\_2025Code\PlayerTaskX\core\src\main\java\com\playerPlugin\playerTaskX\commands\admin\CreateCmd.java




toolName: view_files

status: success


filePath: d:\_yvmou\GitHub\minecraft-plugin\_2025Code\PlayerTaskX\core\src\main\java\com\playerPlugin\playerTaskX\commands\user\MeCmd.java




toolName: view_files

status: success


filePath: d:\_yvmou\GitHub\minecraft-plugin\_2025Code\PlayerTaskX\core\src\main\java\com\playerPlugin\playerTaskX\event\listeners\KillListener.java




toolName: view_files

status: success


filePath: d:\_yvmou\GitHub\minecraft-plugin\_2025Code\PlayerTaskX\core\src\main\java\com\playerPlugin\playerTaskX\event\PlayerJoinHandler.java




toolName: view_files

status: success


filePath: d:\_yvmou\GitHub\minecraft-plugin\_2025Code\PlayerTaskX\api\src\main\java\com\playerPlugin\playerTaskX\api\model\TaskObjective.java




toolName: view_files

status: success


filePath: d:\_yvmou\GitHub\minecraft-plugin\_2025Code\PlayerTaskX\core\src\main\java\com\playerPlugin\playerTaskX\web\TaskEditorController.java




toolName: view_files

status: success


filePath: d:\_yvmou\GitHub\minecraft-plugin\_2025Code\PlayerTaskX\core\src\main\java\com\playerPlugin\playerTaskX\web\ApiRoutes.java




toolName: search_by_regex

status: success

query: catch\s*\(\w*\s+\w+\)\s*\{\s*\}




toolName: search_by_regex

status: success

query: printStackTrace




toolName: search_by_regex

status: success

query: System\.out\.println




toolName: view_files

status: success


filePath: d:\_yvmou\GitHub\minecraft-plugin\_2025Code\PlayerTaskX\api\src\main\java\com\playerPlugin\playerTaskX\api\model\TaskDefinition.java




toolName: view_files

status: success


filePath: d:\_yvmou\GitHub\minecraft-plugin\_2025Code\PlayerTaskX\core\src\main\java\com\playerPlugin\playerTaskX\event\TaskRouter.java




toolName: view_files

status: success


filePath: d:\_yvmou\GitHub\minecraft-plugin\_2025Code\PlayerTaskX\core\src\main\java\com\playerPlugin\playerTaskX\storage\mysql\MySQLTaskProgressRepository.java




toolName: view_files

status: success


filePath: d:\_yvmou\GitHub\minecraft-plugin\_2025Code\PlayerTaskX\core\src\main\java\com\playerPlugin\playerTaskX\storage\json\JsonTaskProgressRepository.java




toolName: view_files

status: success


filePath: d:\_yvmou\GitHub\minecraft-plugin\_2025Code\PlayerTaskX\core\src\main\java\com\playerPlugin\playerTaskX\storage\yaml\YamlTaskProgressRepository.java




toolName: view_files

status: success


filePath: d:\_yvmou\GitHub\minecraft-plugin\_2025Code\PlayerTaskX\api\src\main\java\com\playerPlugin\playerTaskX\api\storage\TaskProgressRepository.java




toolName: view_files

status: success


filePath: d:\_yvmou\GitHub\minecraft-plugin\_2025Code\PlayerTaskX\core\src\main\java\com\playerPlugin\playerTaskX\storage\sqlite\SQLiteRepositoryCreator.java




toolName: view_files

status: success


filePath: d:\_yvmou\GitHub\minecraft-plugin\_2025Code\PlayerTaskX\core\src\main\java\com\playerPlugin\playerTaskX\web\EditorServer.java




toolName: view_files

status: success


filePath: d:\_yvmou\GitHub\minecraft-plugin\_2025Code\PlayerTaskX\core\src\main\java\com\playerPlugin\playerTaskX\cache\TaskCache.java




toolName: view_files

status: success


filePath: d:\_yvmou\GitHub\minecraft-plugin\_2025Code\PlayerTaskX\core\src\main\java\com\playerPlugin\playerTaskX\impl\TaskAPIImpl.java

通过深入分析 `core` 模块，我发现该模块目前处于**严重的半成品状态**，核心功能大量缺失，且已实现部分存在重大性能隐患和代码规范问题。

以下是详细的代码质量分析报告：

### 1. Command 包分析 (`commands` 包)
**现状：**
- 使用了 `cn.yvmou.ylib` 的注解式命令注册，结构较为整洁。
- `CreateCmd` 和 `MeCmd` 是主要实现的命令。

**问题：**
- **逻辑不健壮**：`CreateCmd.java` 中直接访问 `args[1]` 而未先检查 `args.length`，容易导致 `ArrayIndexOutOfBoundsException`。
- **测试代码残留**：`CreateCmd` (L69-72) 中包含硬编码的测试数据（如描述为 "这是描述哈"），未清理。
- **硬编码**：`MeCmd` 中大量使用硬编码的中文消息和 `§` 颜色代码，未抽离到语言文件，不利于国际化和维护。

### 2. Listener 包分析 (`event/listeners` 包)
**现状：**
- `KillListener` 采用了委托模式，将逻辑转发给 `TaskRouter`，设计思路良好。
- `PlayerJoinHandler` 负责在玩家进服时加载数据。

**重大隐患：**
- **主线程 IO 阻塞**：`PlayerJoinHandler.onPlayerJoin` 调用了 `progressRepository.findAll(player)`。在 `YamlTaskProgressRepository` 的实现中，这会遍历文件系统读取文件。**所有 IO 操作均在主线程执行**，当玩家数据较多或磁盘负载高时，会导致服务器瞬间卡顿。
- **逻辑未实现**：`KillListener` 委托给 `TaskRouter`，但 `TaskRouter` 类几乎是空的（见下文）。

### 3. TaskObjective 定义与实现 (`api/model` 包)
**现状：**
- `TaskObjective` 是一个不可变对象（Immutable Object），利于线程安全。

**限制：**
- **扩展性差**：采用了扁平化设计（所有属性都在一个类里）。如果未来需要扩展不同类型的目标（如“到达某地”需要坐标，“对话”需要 NPC ID），目前的结构难以支持，只能通过塞入 `target` 字符串来变通，缺乏多态设计（如 `Objective` 接口 + 多种实现类）。

### 4. 代码中的坏味道 (Bad Smells)
**严重问题：**
- **核心逻辑缺失**：
    - `TaskAPIImpl` 中超过 70% 的核心方法（如 `completeTask`, `incrementTaskProgress`）被**全部注释掉或直接返回 false**。
    - `TaskRouter.dispatchKill` 方法内部为空，导致杀怪任务无法触发。
    - `MySQLTaskProgressRepository` 和 `JsonTaskProgressRepository` 是**空类**。
- **性能杀手**：
    - `TaskRouter` 设计了一个全局 `activeTasks` 列表，意图在事件发生时遍历所有玩家的所有任务。这是 $O(N \times M)$ 的复杂度，且未做线程同步，极易引发 `ConcurrentModificationException`。
    - `TaskCache` 中 `equals/hashCode` 未重写（代码中有 TODO 标记），导致 `List.contains` 检查失效，任务会被重复添加到缓存，造成内存泄漏和逻辑错误。
- **异常处理**：
    - `YamlTaskProgressRepository` (L233) 存在空的 `catch (IOException ignored)`。
    - `SQLiteRepositoryCreator` (L62) 使用了 `e.printStackTrace()` 而非 Logger。
    - `EditorServer` (L148) 使用了 `System.out.println`。

### 5. 前后端交互 (`web` 包)
**现状：**
- 使用 Javalin 框架构建 HTTP 服务。

**问题：**
- **混合逻辑**：`ApiRoutes` 中混合了路由定义和业务逻辑（L22, L35），应统一抽取到 Controller。
- **安全风险**：`EditorServer` (L50) 配置了 `cors.addRule(CorsPluginConfig.CorsRule::anyHost)`，允许任意跨域请求，在生产环境中极其危险。
- **缺乏校验**：直接将 Request Body 反序列化为对象，未进行有效性检查。

### 总结建议
该模块目前**不可用**。建议立即停止新功能开发，优先修复以下问题：
1.  **补全核心逻辑**：实现 `TaskAPIImpl` 和 `TaskRouter` 的基础功能。
2.  **重构 IO 模型**：将 `PlayerJoinHandler` 中的数据加载移至异步线程 (`runTaskAsynchronously`)，加载完成后再回调主线程更新缓存。
3.  **修复 Cache**：必须重写 `TaskProgress` 的 `equals` 和 `hashCode` 方法。
4.  **清理坏味道**：移除测试代码，规范日志和异常处理。


这是一个非常典型的“Demo 级”项目——想法很好（前后端分离、可视化编辑），但实现上漏洞百出。要把它变成一个成熟、可用的插件，需要进行一次**彻底的重构**。

这份文档将作为你的**架构师指南**，分阶段指导你如何将这个“半成品”打磨成高质量的开源项目。

---

# 🛠️ PlayerTaskX 重构与开发路线图

## 1. 现状诊断 (Why Refactor?)

在开始写代码之前，必须先认清当前项目的致命问题。如果不解决这些，后续功能开发就是堆砌屎山。

| 维度 | 严重问题 | 后果 |
| :--- | :--- | :--- |
| **架构** | **主线程 I/O** (文件读写在主线程) | 玩家进服或保存数据时，服务器会瞬间卡顿 (Lag)。 |
| **核心** | **逻辑缺失** (`TaskAPIImpl` 大量留白) | 任务无法完成、进度无法更新，插件基本不可用。 |
| **扩展** | **模型僵化** (`TaskObjective` 扁平设计) | 无法轻易添加新类型的任务（如：对话任务、挖掘任务）。 |
| **安全** | **Web 裸奔** (CORS 全开, 无校验) | 任何人都可以通过接口修改服务器数据，甚至注入恶意代码。 |
| **规范** | **代码随意** (硬编码、异常吞没) | 难以维护，Debug 极其困难，日志系统形同虚设。 |

---

## 2. 重构路线图 (Roadmap)

我们将开发过程分为 5 个里程碑（Milestone）。**不要试图一次性做完所有事情**，按部就班地来。

### 📅 阶段一：基础设施修复 (v0.1.0-alpha)
**目标**：让插件“跑得稳”，解决卡顿和报错，建立规范。

1.  **异步 I/O 改造**：
    *   引入 `CompletableFuture` 或 Bukkit 的 `runTaskAsynchronously`。
    *   **重构 `YamlTaskProgressRepository`**：所有 `load`, `save` 方法必须在异步线程执行。
    *   **重构 `PlayerJoinHandler`**：玩家进服 -> 异步加载数据 -> 回调主线程更新缓存。
2.  **依赖注入与单例清理**：
    *   目前的 `PlayerTaskX` 类太臃肿。引入轻量级依赖注入（或者手动管理），将 `Repository`, `Service`, `Controller` 分离。
3.  **日志与异常规范**：
    *   **禁止**使用 `e.printStackTrace()` 和 `System.out.println`。
    *   统一使用 `YLib` 的 `Logger`，并封装统一的 `TaskException` 异常类。
4.  **配置管理重构**：
    *   将硬编码的字符串（如命令提示、错误信息）全部移入 `messages.yml`。

### 📅 阶段二：核心逻辑补全 (v0.2.0-beta)
**目标**：让任务“能动起来”，实现任务的创建、接受、更新和完成。

1.  **重构 `TaskDefinition` & `TaskObjective`**：
    *   **痛点**：目前的 `TaskObjective` 是个大杂烩。
    *   **方案**：改为多态设计。
        ```java
        public abstract class TaskObjective {
            protected String id;
            protected int amount;
            public abstract boolean check(Event event); // 核心判断逻辑
        }
        public class KillMobObjective extends TaskObjective { ... }
        public class BreakBlockObjective extends TaskObjective { ... }
        ```
    *   使用 Jackson 的 `@JsonTypeInfo` 处理多态序列化。
2.  **实现 `TaskAPIImpl`**：
    *   填补 `updateProgress`, `completeTask`, `resetTask` 的逻辑。
    *   确保状态变更后，立即（异步）触发持久化保存。
3.  **实现 `TaskRouter` (事件分发中心)**：
    *   这是任务系统的**心脏**。
    *   监听 Bukkit 事件 -> 筛选相关玩家 -> 遍历玩家的进行中任务 -> 检查目标 -> 更新进度。

### 📅 阶段三：功能扩展 (v0.3.0-feature)
**目标**：丰富玩法，支持多种任务类型和奖励。

1.  **多种任务目标支持**：
    *   实现：杀怪 (EntityDeathEvent)、破坏 (BlockBreakEvent)、放置 (BlockPlaceEvent)、对话 (PlayerInteractEntityEvent)。
2.  **奖励系统 (Reward System)**：
    *   设计 `Reward` 接口。
    *   实现：指令奖励、物品奖励、经验奖励、Vault 经济奖励。
3.  **条件系统 (Condition System)**：
    *   接受任务的前置条件：等级限制、权限限制、前置任务限制。

### 📅 阶段四：前后端对接 (v0.4.0-web)
**目标**：打通 Web 编辑器，实现可视化配置。

1.  **API 接口标准化**：
    *   设计 RESTful API：`GET /api/tasks`, `POST /api/tasks`, `DELETE /api/tasks/{id}`。
    *   使用 DTO (Data Transfer Object) 隔离内部模型和 API 数据。
2.  **安全加固**：
    *   **鉴权**：实现简单的 Token 认证或 OP 权限验证，防止未授权访问。
    *   **校验**：对前端传来的 JSON 进行严格校验（如 ID 是否合法、数值是否为负）。
3.  **前端适配**：
    *   更新 React 前端以匹配新的后端 API 和数据结构。

### 📅 阶段五：发布准备 (v1.0.0-release)
**目标**：稳定、文档齐全，准备开源发布。

1.  **测试**：
    *   编写单元测试（JUnit 5 + Mockito）覆盖核心逻辑。
    *   进行压力测试（模拟 50-100 人同时在线更新任务）。
2.  **文档**：
    *   编写 `README.md`（功能介绍、安装指南）。
    *   编写 `WIKI`（配置教程、API 文档）。
3.  **发布**：
    *   构建 Jar 包，发布到 GitHub Releases 和 SpigotMC/MineBBS。

---

## 3. 实战：第一步该做什么？

别急着写代码，先整理你的 **文件结构**。现在的包结构太乱了。

**推荐的包结构：**
```text
com.playerPlugin.playerTaskX
├── api                 // [API模块] 对外接口
│   ├── model           // 数据模型 (Task, Objective, Reward)
│   ├── event           // 自定义事件
│   └── service         // 核心服务接口 (TaskService)
├── core                // [Core模块] 核心实现
│   ├── command         // 命令处理
│   ├── config          // 配置管理
│   ├── listener        // Bukkit事件监听
│   ├── manager         // 逻辑管理器 (TaskManager, RewardManager)
│   ├── storage         // 数据存储 (Repository实现)
│   │   ├── json
│   │   └── mysql
│   └── web             // Web服务器 (Controller)
└── PlayerTaskX.java    // 插件入口
```

## 4. 给开发者的建议

1.  **提交规范**：每次提交只做一件事。
    *   `feat: 添加杀怪任务目标支持`
    *   `fix: 修复玩家离线时数据未保存的 bug`
    *   `refactor: 重构 TaskAPIImpl 使用异步保存`
2.  **不要重复造轮子**：
    *   利用好 `YLib` 提供的功能。
    *   数据库连接池用 `HikariCP`。
    *   JSON 处理用 `Jackson` 或 `Gson`。
3.  **保持简单 (KISS)**：
    *   先实现最简单的“杀 10 只僵尸奖励 1 个钻石”，跑通全流程，再考虑复杂的逻辑。

---

**下一步指令：**
如果你准备好了，请告诉我 **“开始阶段一”**，我将带你先解决最紧迫的 **异步 I/O 和数据存储重构** 问题。