# 示例预设：随插件发布（jar 内的 presets/），只在 presets/ 目录空着时铺一份到这里，之后插件不再动它。
# 文件名（去掉扩展名）就是 id，kind 决定它是目标预设还是奖励预设。
# 预设由任务里的 preset: 引用、载入时展开成生效值；删掉它，引用它的任务会被校验报出来。
# 不需要就删掉——目录非空时不会再补。

kind: objectives
type: fish
properties:
  amount: 10
  target: ''
