# 示例预设：随插件发布（jar 内的 presets/），只在 presets/ 目录空着时铺一份到这里，之后插件不再动它。
# 文件名（去掉扩展名）就是 id，kind 决定它是目标预设还是奖励预设。
# 预设只是编辑器的便利设施，引擎不认识它，删掉不影响任何已建好的任务；
# 不需要就删掉——目录非空时不会再补。

kind: rewards
name: 奖励 3 个钻石
type: command
description: 用命令发物品，不需要任何依赖
properties:
  command: give %player% diamond 3
