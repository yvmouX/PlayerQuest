完善网页编辑器的editor界面

任务类型有循环任务、定时任务，永久任务，限时任务

需要的节点：

1.起始节点

2.需要以下任务节点，并可调整参数

```
/**
 * 表示任务的执行类型，不同类型决定任务的状态流转规则与重置机制。
 *
 * <p><b>1. CYCLE（循环任务）</b></p>
 * <ul>
 *     <li>任务完成后自动重置，可重复执行。</li>
 *     <li>状态流转：
 *         NOT_STARTED → IN_PROGRESS → COMPLETED → NOT_STARTED → ...
 *     </li>
 * </ul>
 *
 * <p><b>2. TIMER（定时任务）</b></p>
 * <ul>
 *     <li>任务按照固定时间间隔强制重置为 NOT_STARTED。</li>
 *     <li>重置行为与当前状态无关。</li>
 *     <li>通常依赖外部调度器（如定时任务 / Tick / Scheduler）。</li>
 * </ul>
 *
 * <p><b>3. FOREVER（永久任务）</b></p>
 * <ul>
 *     <li>任务仅能完成一次，完成后不再重置。</li>
 *     <li>状态流转：
 *         NOT_STARTED → IN_PROGRESS → COMPLETED
 *     </li>
 * </ul>
 *
 * <p><b>4. LIMIT（限时任务）</b></p>
 * <ul>
 *     <li>必须在指定时间内完成。</li>
 *     <li>若超时未完成，则状态变为 EXPIRED。</li>
 *     <li>状态流转：
 *         NOT_STARTED → IN_PROGRESS → COMPLETED
 *         或
 *         NOT_STARTED / IN_PROGRESS → EXPIRED
 *     </li>
 * </ul>
 *
 * <p><b>5. NONE（无效任务）</b></p>
 * <ul>
 *     <li>用于表示未知或解析失败的任务类型。</li>
 *     <li>不应参与任何业务逻辑。</li>
 * </ul>
 */
```

3.完成节点

​	显式结束任务

​	可触发奖励 / 回调

4.条件节点

用于：

- 判断玩家状态
- 判断任务条件是否达成

例：

- 是否击杀10只怪
- 是否拥有某物品
- 是否达到等级



## 5.分支节点（Branch / If）

配合 Condition 使用：

```
IF 条件成立 → A
ELSE → B
```

👉 没这个你只能做线性任务（很废）

## 6.执行节点（Action Node）

用于真正做事情：

比如：

- 发奖励
- 扣物品
- 播动画
- 发送消息

```
[Action]
- type: GIVE_ITEM
- item: diamond
- count: 3
```

## 7.监听节点（Event / Trigger Node）

👉 这个是你现在**最缺的**

用于：

- 玩家行为触发任务

比如：

- 击杀事件
- 进入区域
- 点击NPC

```
[Trigger]
- type: PLAYER_KILL
```



## 计数节点（Counter Node）

用于累计进度：

```
击杀 0/10
收集 3/5
```

👉 不然你所有任务都只能“一次完成”

------

## 8️⃣ 定时控制节点（Timer Control）

你现在有 TIMER 类型，但缺“节点级控制”：

- 延迟执行
- 冷却时间
- 周期执行

```
[Timer]
- delay: 5s
- repeat: true
```

------

## 9️⃣ 状态节点（State Node）

用于：

- 手动切换任务状态
- 强制失败 / 完成

👉 很适合GM工具 / 调试

------

## 🔟 子任务节点（Sub Task / Group）

👉 做复杂任务必须有

允许：

- 嵌套任务
- 组合任务

例：

```
主任务
 ├── 子任务A
 ├── 子任务B
```