# PlayerTaskX 使用文档

一个面向 Spigot / Paper / Folia / Canvas 的**周期任务与自定义任务**插件：
任务由「多个目标 + 多个奖励」组成，支持网页可视化编辑、游戏内 GUI、多语言与变量。

---

## 从这里开始

| 文档 | 内容 |
|---|---|
| [快速开始](quick-start) | 安装、首次配置、5 分钟跑通第一个任务 |
| [命令](commands) | 玩家命令与管理员命令的完整清单 |
| [配置](configuration) | `config.yml` 每一项的含义 |
| [任务目标](objectives) | 15 种目标类型与各自的配置字段 |
| [任务奖励](rewards) | 3 种奖励类型与发放规则 |
| [网页编辑器](editor) | 浏览器里创建与管理任务、查看玩家进度 |
| [变量](placeholders) | PlaceholderAPI 变量清单 |
| [常见问题](faq) | 排查「任务不涨进度」「奖励拿不到」等 |

---

## 核心概念

### 任务（Quest）

一个任务 = **若干目标 + 若干奖励**。所有目标都完成后任务标记为「已完成」，
玩家在 GUI 或命令里**主动领取**奖励。

```
任务「挖矿日常」
├── 目标 1：挖掘 64 个石头
├── 目标 2：发言一次（内容包含「你好」）
└── 奖励 1：500 金币
```

### 任务类型

| 类型 | 说明 |
|---|---|
| `DAILY` 每日任务 | 从任务池按玩家抽取，每天重置，可用货币刷新 |
| `WEEKLY` / `MONTHLY` | 同上，按周 / 按月换一批（重置锚点在配置里） |
| `CUSTOM` 自定义周期 | 周期长度自己定（如每 3 天 / 每 12 小时），按固定锚点取整 |
| `NORMAL` 普通任务 | 常驻任务；**目前还没有发给玩家的入口**（只在编辑器与 `/ptxa list` 里可见） |

四种周期**各自独立发放**：一个玩家可以同时有 3 个每日任务和 2 个每周任务，
每种各有自己的数量、重置锚点、刷新费用与上限（见 [配置 · 周期任务](configuration?id=周期任务每日--每周--每月--自定义)）。

### 数据存储

任务定义、预设与玩家进度**都存在同一个数据库里**：默认 **SQLite**（零配置、单文件，
开箱即用），多服共享玩家数据时换成 **MySQL**。详见 [配置](configuration)。

除此之外还可以把任务/预设写成 `quests/`、`presets/` 下的 YAML 文件，作为**只读**来源补充进来
（数据库仍是唯一可写的那一份）。

---

## 特性一览

- **15 种任务目标**：挖掘、放置、合成、垂钓、自定义钓鱼、击杀、消耗、附魔、剪切、繁殖、驯服、交互、发言、提交、执行命令
- **3 种任务奖励**：金币（经 Vault / VaultUnlocked 的经济服务）、点券（PlayerPoints）、自定义命令
  （发物品、经验、其它插件的东西都走它，例如 `give %player% diamond 3`）
- **货币可配置**：刷新费用的货币顺序在配置里指定（默认「金币 → 点券」），
  两种货币一个都没有时刷新会明确提示不可用（`refresh-cost: 0` 则是免费刷新）
- **完全自定义**：一个任务可挂任意多个目标与奖励，参数由类型自身的 schema 描述
- **插件联动**：装了 **MythicMobs** 时击杀任务可指定自定义怪（`mythic:<怪物id>`，
  编辑器里能直接选）；装了 **CustomFishing** 时可用「自定义钓鱼」目标按鱼 id 与尺寸统计钓获；
  装了 **ItemsAdder** / **CraftEngine** 时方块与物品目标可写它们的自定义 id
  （`itemsadder:<id>` / `craftengine:<id>`，裸 id 也认），自定义方块同样计入挖掘与放置
- **网页编辑器**：任务列表（搜索/筛选/排序/批量操作）、表单编辑、**YAML 文本编辑**、
  玩家进度、YAML 导入导出
- **定义也能写在文件里**：`quests/` 与 `presets/` 下的 YAML 是**只读来源**，适合随插件发布、
  进版本控制；空着时插件会铺一份示例（`example_` 前缀，取自插件自带的资源），数据库优先，
  同 id 冲突时告警并忽略文件里那份
- **游戏内 GUI**：玩家周期任务界面（标签切换四种周期）、任务详情、管理员任务管理
- **进度提示**：动作栏实时显示进度，任务完成时弹出标题提醒
- **多语言**：内置简中 / 英文，可自行增补语言文件
- **变量**：与 PlaceholderAPI 集成，可在计分板、菜单里展示任务信息
- **跨平台调度**：基于 YLib 的通用调度器，同一份代码在 Spigot / Paper / Folia / Canvas 上运行

---

## 运行要求

| 项目 | 要求 |
|---|---|
| 服务端 | Spigot / Paper / Folia / Canvas，1.21 及以上（`plugin.yml` 的 `api-version: '1.21'`） |
| Java | 21 |
| 软依赖（可选） | 经济插件（金币奖励：Vault / VaultUnlocked + EssentialsX、CMI 等）、PlayerPoints（点券奖励）、PlaceholderAPI（变量） |

> 软依赖缺失**不会导致插件无法启动**：对应的奖励类型会被标记为「不可用」，
> 并在启动日志与管理员界面里明确提示原因。

---

## 开发与构建

```powershell
.\start-folia.ps1              # 构建 + 复制到 run\plugins + 启动测试服
.\start-folia.ps1 -Clean       # 先 clean 再构建
.\start-folia.ps1 -Foreground  # 前台运行，可直接输入服务端命令
.\gradlew.bat build            # 只构建与跑测试
```

构建产物为 `build/libs/playerTaskX-<版本>-all.jar`（**部署请用这个 `-all` 版本**）。

架构说明见 [ARCHITECTURE.md](https://github.com/yvmouX/PlayerTaskX/blob/main/ARCHITECTURE.md)。
